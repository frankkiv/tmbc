import React from 'react';

import './stat-player.module.scss';

/* eslint-disable-next-line */
export interface StatPlayerProps {}

export function StatPlayer(props: StatPlayerProps) {
  return (
    <div>
      <h1>Welcome to StatPlayer!</h1>
    </div>
  );
}

export default StatPlayer;
